# 开发日志 (DEVLOG)

按日期倒序记录每天的**代码改动**——bug 修复、功能调整、参数变更。
面向"这天系统改了什么代码",供接手的 agent 或自己快速对齐近期变更。

## 与其他文档的分工

| 文档 | 记什么 | 粒度 |
|---|---|---|
| **DEVLOG.md(本文件)** | 每天的代码改动(bug/功能/参数) | 按日期 |
| `CHANGELOG.md` | 版本发布的功能特性 | 按版本/功能 |
| `cognition/findings.md` | 回测验证的实证结论 | 按主题 |
| `cognition/daily/` | 探索性认知/发现(非代码) | 按日期 |
| `CLAUDE.md` | 项目架构长期说明 | — |

> 简单判断: **改了代码 → DEVLOG; 验证了结论 → findings; 探索了想法 → cognition/daily。**

## 更新规则

1. **每天一节**,标题 `## YYYY-MM-DD`。
2. 每条改动: `类型` 标签 + 一句话说明 + 受影响文件(`文件:行`)。
3. 类型: `[FIX]` bug修复 / `[FEAT]` 新功能 / `[TUNE]` 参数或逻辑调整 / `[REFACTOR]` 重构 / `[DATA]` 数据修正(纯数据无代码可不记,除非影响逻辑)。
4. 倒序排列(最新日期在最上面)。
5. 只记**实际改动**,不记"计划/想法"(那些去 cognition/daily 或 issue)。

---

## 2026-06-26

### [FIX] 归因 prompt bug: 选项被推理模型复述致空壳/低质量归因
- **根因**: `ATTR_PROMPT_UNIFIED` 把 REASON_TYPE 选项塞进模板行(`REASON_TYPE|板块供需 或 个股事件 或...`), GLM-5.2 推理模型常当"已填好的示例"原样复述 → 17 只空壳(summary 空)+ 4 只低质量(reason_type 含"或")。
- **修复**: ① 选项移出模板行进说明、模板行用"这里写..."纯文字占位(去掉会被复述的 `<...>`)、"第一行就是答案禁止分析"强约束; ② 归因调用从 `_llm_quick`(不重试, max_tokens=300 推理不够) 换 `_llm`(带 429 重试, max_tokens=1500)。
- **结果**: 重跑受影响归因, 159/159 全有效(空壳 0、低质量 0)。
- **文件**: `picker/discovery/attribution.py:54-64`(prompt)、`:38`(import)、`:314-315`(调用)
- **教训**: 给推理模型写 prompt, 示例占位符(`<...>`、选项列表)会被当答案复述; 占位符要用"这里写..."纯指令文字并显式禁止复述。

### [TUNE] chain_tiers 异动信号取消截断(全量进 prompt)
- **问题**: `price_confirmed_hot[:20]` / `cold[:10]` 截断前未按信号强度排序, 按 JSON 字典序随机砍。实测 76 涨 + 26 跌, 被砍 56 涨 + 16 跌(74%/62%), 含涨幅最大的三孚股份(+64%)、聚石化学(+59%)。
- **修复**: 删除两行截断, 全量进 prompt。无需调 max_tokens——异动信号是输入(全量 ~3000 token, 占 128K 上下文 <3%), max_tokens 管的是输出(tier_map 固定 6 档结构, 不增大)。
- **效果**: 被砍大涨股所在赛道现在能进相应热度档(高纯四氯化硅/先进封装电镀/PCB填孔药水/石英晶振等)。
- **文件**: `picker/scoring/chain_tiers.py:383-384`(删除, 替换为说明注释)
- **教训**: 截断必须按强度排序, 否则等于随机抽样; 且阈值要对照实际数据量评估。

### [TUNE] 下跌异动股不再触发 fundamentals 刷新
- **变更**: `refresh_from_research` 合并异动股时跳过 `direction=='下跌'`, 刷新列表只含"研报提及 ∪ 上涨异动"。实测 193 → 175 只。
- **设计**: 下跌股用途是感知行业动向——喂给 `chain_tiers._gather_research_signals` 的 `price_confirmed_cold` 发现板块风险(该链路与 fundamentals 刷新独立, 不受影响)。若某下跌股因研报提及/上涨异动触发而进入刷新, 其下跌结论在 `refresh_one` 内仍作为 headwinds 注入。
- **文件**: `picker/pipeline/refresh_fundamentals.py:721-749`

### [DATA] 热池冷门清理: 32 只弱势股移入冷池
- **操作**: 执行 `cleanup_to_cold_stocks`(现有 Step 6.5 机制, V3<7+chain<4+cap<3+r20<5+无研报)。热池 578→546, 冷池 200→232。
- **被清**: 煤炭/油气/有色(中国神华/中国石油/中煤能源等)、白酒消费(贵州茅台/泸州老窖/山西汾酒等, 含蓝筹按用户选择一并清)、光伏(晶科/大全/阿特斯)等近20日全下跌(-4.5%~-38%)、无研报关注的退潮股。
- **说明**: 纯数据操作无代码改动, 记此条因影响池子边界; 三步同步已校验一致(fundamentals/V3 cache/cold_stocks.json)。
